const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const appsToUpdate = await prisma.application.findMany({ where: { status: { notIn: ['VALIDATED', 'SUBMITTED', 'APPROVED', 'REJECTED'] }, procedures: { some: { isLocked: true } } } }); console.log('Found ' + appsToUpdate.length + ' legacy applications to mark as SUBMITTED'); for (const app of appsToUpdate) { await prisma.application.update({ where: { id: app.id }, data: { status: 'SUBMITTED' } }); } console.log('Migration complete.'); } main().catch(console.error).finally(() => prisma.$disconnect());
